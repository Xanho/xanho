name: frontend
description: The frontend application for Xanho.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  protobuf: ^1.0.1
  grpc: ^2.3.0
  provider: ^4.3.2+2
  firebase_core: "^0.5.0+1"
  firebase_auth: "^0.18.1+2"

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.3
  build_web_compilers: ^2.12.2
  test: ^1.15.4

flutter:
  uses-material-design: true
